#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

const char ALPHABET_FIRST = 'A'; // necessary special character for suffix sorting
const char ALPHABET_LAST = 'Z';
const int ALPHABET_SIZE = 4; // remember: +1 for @
const int PREF_WIDTH = 2; // used in parallel suffix sort

#endif
